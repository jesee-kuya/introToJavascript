const isValid = (date) => {
    return isValid(date)
}

const isAfter = (date, date1) => {
    return isAfter(date1, date)
}

const isBefore = (date, date1) => {
    return isBefore(date1, date)
}

const isFuture = (date) => {
    return isFuture(date)
}

const isPast = (date) => {
    return isPast(date)
}